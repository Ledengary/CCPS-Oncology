name: ccpsonc
channels:
  - defaults
dependencies:
  - python=3.10.19
  - pip=25.3
  - pip:
      # Core scientific stack
      - numpy==2.2.6
      - pandas==2.3.3
      - scipy==1.15.3
      - scikit-learn==1.7.2
      - matplotlib==3.10.7
      - seaborn==0.13.2
      - tqdm==4.67.1

      # Deep learning + LLM serving
      - torch==2.8.0
      - torchvision==0.23.0
      - torchaudio==2.8.0
      - transformers==4.57.1
      - accelerate==1.12.0
      - vllm==0.11.0
      - peft==0.18.1
      - datasets==4.4.0
      - safetensors==0.6.2

      # Test-set generation (OpenAI for GPT-5.1)
      - openai==2.8.0

      # Notebooks
      - jupyter==1.1.1
      - nbformat==5.10.4
      - ipykernel==6.30.1

      # File I/O
      - openpyxl==3.1.5
      - python-docx==1.2.0
